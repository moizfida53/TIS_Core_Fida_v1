@{
    ViewData["Title"] = "Re-Assign Bill";
    Layout = "_Layout";
}

<div class="d-flex align-items-center mb-3">
    <h5 class="fw-bold mb-0" style="color:var(--bs-primary)">
        <i class="fas fa-user-edit me-2"></i>Re-Assign Bill
    </h5>
</div>

<div class="tis-card">
    <p class="text-muted">Re-Assign Bill view — implementation pending.</p>
</div>
