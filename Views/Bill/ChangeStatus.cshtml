@{
    ViewData["Title"] = "Change Bill Status";
    Layout = "_Layout";
}

<div class="d-flex align-items-center mb-3">
    <h5 class="fw-bold mb-0" style="color:var(--bs-primary)">
        <i class="fas fa-exchange-alt me-2"></i>Change Bill Status
    </h5>
</div>

<div class="tis-card">
    <p class="text-muted">Change Status view — implementation pending.</p>
</div>
